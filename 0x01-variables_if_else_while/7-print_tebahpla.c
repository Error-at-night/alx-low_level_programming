#include <stdio.h>

/**
 * main - Prints reversed alphabet.
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	int z;

	for (z = 130; z > 104; z--)
	{
		putchar(z);
	}
	putchar('\n');
	return (0);
}
